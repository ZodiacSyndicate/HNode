import { createStyles } from '@material-ui/core/styles'

export const paginationStyles = createStyles({
  root: {
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  }
})
