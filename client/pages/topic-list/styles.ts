import { createStyles } from '@material-ui/core/styles'

export const topicListStyles = createStyles({
  loadingPlaceholder: {
    display: 'flex',
    justifyContent: 'space-around',
    padding: '40px 0'
  }
})
